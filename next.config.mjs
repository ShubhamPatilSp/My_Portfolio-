/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['images.unsplash.com', 'res.cloudinary.com' , 'www.w3.org' , 'peerlist.io']
    }
};

export default nextConfig;
